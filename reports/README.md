# Reports

本目录用于存放**人工分析报告、阶段性记录、交叉检查结果与非正式研究输出**。

## 目录约定

- `notes/`：工作日志、阶段记录、临时 TODO、会话总结
- `data/`：数据交叉检查、对账、抽检类报告

## 使用原则

1. 这里放的是**过程性或分析性材料**，不是系统运行输入。
2. 正式架构/治理/规范文档优先放在顶层 `docs/`。
3. 若报告已经失效但仍需保留，可后续移入顶层 `archive/`。
